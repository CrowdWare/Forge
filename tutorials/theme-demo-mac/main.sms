// Sat Nam

var isDark = 0

on mainWindow.ready() {
    log.success("Ready")
    ui.setTheme("light")
}

on btnTheme.clicked() {
    log.success("Theme")
    if (isDark == 0) {
        isDark = 1
        var btn = ui.getObject("btnTheme")
        btn.text = "Light Mode"
        ui.setTheme("dark")
    } else {
        isDark = 0
        var btn = ui.getObject("btnTheme")
        btn.text = "Dark Mode"
        ui.setTheme("light")
    }
}

on btnAnimate.clicked() {
    log.success("Animate")
    ui.animate("animBox", "scale", 1.4, 200)
}
